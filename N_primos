#include <stdio.h>

int main() {
    int i, num, soma=0;
  
  printf("Informe um numero"); 
  scanf("%d",&num);
  
    for(i=1;i<= num;i++){
      if(num%i==0)
        soma+=1;
  }
    if(soma==2)
      printf("Primo");
    else
      printf("Comum");
 
}
